# Secure_Password_Manager
A Python-based secure password manager that allows users to safely store and manage credentials using encryption. The application uses a master password to protect the vault, generates strong random passwords, and ensures that no passwords are stored in plain text. It supports basic CRUD operations with a strong focus on security and data privacy.
